export interface characterCardInterface {
  name: string;
  nickName: string;
  imgUrl: string;
  desc: string;
}

export interface topCharacterRowInterface {
  name: string;
  skillset: string;
  votes: number;
  bgColor: string;
}
